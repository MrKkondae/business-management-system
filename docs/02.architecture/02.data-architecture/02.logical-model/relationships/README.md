# 엔터티 관계

업무영역별 관계 CSV와 영역 간 관계 CSV를 관리한다. 관계 하나를 한 행으로 관리하며 각 CSV가 해당 범위 엔터티 관계의 원본이다.

`업무대상`은 첨부파일·메모의 `(업무구분, 업무ID)`가 참조하는 여러 업무 엔터티를 통칭하는 논리적 부모이다. 이와 같은 다형 참조는 관계 CSV에는 논리 관계로 정의하되 단일 부모 테이블을 확정할 수 없으므로 물리 FK에서는 제외한다.
