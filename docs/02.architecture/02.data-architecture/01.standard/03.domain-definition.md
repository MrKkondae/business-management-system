# BMS 도메인 정의서

## 1. 문서 개요

### 1.1 목적

본 문서는 BMS(Business Management System) 데이터베이스 설계 시 사용하는 표준 도메인(Domain)을 정의한다.

도메인은 데이터의 형식(Type), 길이(Length), 정밀도(Precision), Null 허용 여부 등을 표준화하여 데이터의 일관성과 재사용성을 확보하기 위한 기준으로 활용한다.

본 문서는 테이블 정의서, ERD, 표준용어 사전의 기준 문서로 사용한다.

---

## 1.2 작성 원칙

* 동일한 의미의 데이터는 동일한 도메인을 사용한다.
* 컬럼은 가능한 표준 도메인을 사용한다.
* 신규 도메인은 기존 도메인 재사용 여부를 우선 검토한다.
* DBMS 변경 시에도 동일한 도메인명을 사용한다.

---

# 2. 문자형 도메인

> 논리 도메인은 `domain-definition.csv`, PostgreSQL 타입은 `db-type-mapping.csv`에서 관리한다.

---

# 3. 날짜 및 시간 도메인

> 논리 도메인은 `domain-definition.csv`, PostgreSQL 타입은 `db-type-mapping.csv`에서 관리한다.

---

# 4. 숫자형 도메인

> 논리 도메인은 `domain-definition.csv`, PostgreSQL 타입은 `db-type-mapping.csv`에서 관리한다.

---

# 5. 금액 도메인

> 논리 도메인은 `domain-definition.csv`, PostgreSQL 타입은 `db-type-mapping.csv`에서 관리한다.

---

# 6. 여부 도메인

> 논리 도메인은 `domain-definition.csv`, PostgreSQL 타입은 `db-type-mapping.csv`에서 관리한다.

허용값

> 논리 도메인은 `domain-definition.csv`, PostgreSQL 타입은 `db-type-mapping.csv`에서 관리한다.

---

# 7. 연락처 도메인

> 논리 도메인은 `domain-definition.csv`, PostgreSQL 타입은 `db-type-mapping.csv`에서 관리한다.

---

# 8. 사업자 정보 도메인

> 논리 도메인은 `domain-definition.csv`, PostgreSQL 타입은 `db-type-mapping.csv`에서 관리한다.

---

# 9. 공통 도메인

> 논리 도메인은 `domain-definition.csv`, PostgreSQL 타입은 `db-type-mapping.csv`에서 관리한다.

---

# 10. 도메인 사용 예시

## 프로젝트

- 컬럼명: PRJ_ID; 도메인: ID
- 컬럼명: PRJ_NM; 도메인: LONG_NAME
- 컬럼명: PRJ_TYPE_CD; 도메인: CODE
- 컬럼명: PRJ_STATUS_CD; 도메인: CODE
- 컬럼명: START_DT; 도메인: DATE
- 컬럼명: END_DT; 도메인: DATE
- 컬럼명: REG_ID; 도메인: REG_ID
- 컬럼명: REG_DTM; 도메인: REG_DTM

---

## 계약

- 컬럼명: CNTR_ID; 도메인: ID
- 컬럼명: CNTR_NO; 도메인: NUMBER
- 컬럼명: CNTR_NM; 도메인: LONG_NAME
- 컬럼명: CNTR_AMT; 도메인: AMOUNT
- 컬럼명: CNTR_DT; 도메인: DATE

---

## 고객

- 컬럼명: CUST_ID; 도메인: ID
- 컬럼명: CUST_NM; 도메인: LONG_NAME
- 컬럼명: BIZ_REG_NO; 도메인: BIZ_REG_NO
- 컬럼명: TEL_NO; 도메인: TEL_NO
- 컬럼명: EMAIL; 도메인: EMAIL

---

# 11. 도메인 관리 원칙

* 신규 컬럼은 기존 도메인을 우선 사용한다.
* 동일한 데이터 형식은 동일한 도메인을 사용한다.
* 도메인 변경 시 표준용어 및 테이블 정의서를 함께 검토한다.
* DBMS별 데이터 타입은 물리 모델 단계에서 조정할 수 있으나 도메인명은 변경하지 않는다.
