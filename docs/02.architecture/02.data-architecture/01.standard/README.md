# 데이터 표준

표준단어, 표준용어, 논리 도메인 및 DBMS별 타입 매핑을 관리한다.

표준단어는 `standard-words.csv`, 논리 도메인은 `domain-definition.csv`, DBMS 타입 매핑은 `db-type-mapping.csv`가 원본이다. 표준용어는 `terms` 아래 업무영역별 CSV가 원본이다. Markdown은 작성 규칙과 관리 원칙만 설명한다.
